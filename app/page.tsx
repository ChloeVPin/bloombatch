import { BloomBatch } from "@/components/bloom-batch"

export default function Page() {
  return (
    <main className="h-screen overflow-hidden bg-background">
      <BloomBatch />
    </main>
  )
}
